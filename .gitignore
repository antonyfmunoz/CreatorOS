node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz.env

# Local dev cruft / DB dumps — never commit.
dump*.sql
New Text Document*.txt
attached_assets/stitch_creatoros/
# Runtime and field-test media stay local or in object storage. Historical
# tracked fixtures remain available, but new uploads must never enter a release.
uploads/*
attached_assets/creativesos-youtube-field-test.mp4
.wrangler/
workers/**/.wrangler/

# WP-P4-SECRETS-001: real env files are secret-bearing — never commit.
.env
.env.*
!.env.op.tpl
!.env.tpl
!.env.example
